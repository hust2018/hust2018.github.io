# 依赖
/node_modules

# Next.js 构建产物
/.next/
/out/

# 调试 / 系统
*.log
.DS_Store

# TypeScript
/next-env.d.ts
*.tsbuildinfo

# 源简历 PDF（含手机号等隐私，不入公开仓库）
*.pdf
# 但生成的简历 PDF（只含邮箱、由 pnpm pdf 生成）要入库并随站点部署
!public/resume.pdf
